# 环境变量配置示例
# 复制此文件为 .env.local 并填入实际的配置值

# ==================== 数据库配置 ====================
# 数据库类型: mysql 或 postgres
DB_TYPE=mysql

# 数据库主机地址
DB_HOST=your-database-host.example.com

# 数据库端口(MySQL默认3306, PostgreSQL默认5432)
DB_PORT=3306

# 数据库名称
DB_NAME=aipibox_sync

# 数据库用户名
DB_USER=your_db_username

# 数据库密码
DB_PASSWORD=your_db_password

# 连接池大小(可选,默认10)
DB_CONNECTION_LIMIT=10

# 是否启用SSL连接(生产环境推荐设为true)
DB_SSL=true

# ==================== Vercel/Netlify 示例配置 ====================

# === Vercel 配置示例 ===
# 1. 在 Vercel 项目设置中添加以上环境变量
# 2. 如果使用 PlanetScale/Railway/Supabase 等托管数据库:
#    - DB_HOST: 从数据库控制台获取连接字符串
#    - DB_SSL: 设为 true
#    - DB_TYPE: mysql 或 postgres

# === Netlify 配置示例 ===
# 1. 在 Netlify 站点设置 > Build & deploy > Environment 中添加变量
# 2. 推荐使用 Supabase(PostgreSQL) 或 PlanetScale(MySQL)

# ==================== 推荐的数据库服务商 ====================

# 1. PlanetScale (MySQL, 免费层可用)
#    DB_TYPE=mysql
#    DB_HOST=xxx.connect.psdb.cloud
#    DB_USER=xxx
#    DB_PASSWORD=pscale_pw_xxx
#    DB_SSL=true

# 2. Supabase (PostgreSQL, 免费层可用)
#    DB_TYPE=postgres
#    DB_HOST=db.xxx.supabase.co
#    DB_PORT=5432
#    DB_USER=postgres
#    DB_PASSWORD=your_password
#    DB_NAME=postgres
#    DB_SSL=true

# 3. Railway (MySQL/PostgreSQL, 免费层可用)
#    从 Railway 项目获取数据库连接信息

# 4. Neon (PostgreSQL, 免费层可用)
#    DB_TYPE=postgres
#    DB_HOST=xxx.neon.tech
#    DB_SSL=true

# ==================== 本地开发配置 ====================
# 如果在本地运行 MySQL/PostgreSQL:
# DB_TYPE=mysql
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=your_local_password
# DB_NAME=aipibox_sync
# DB_SSL=false
